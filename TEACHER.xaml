<Window x:Class="TSSP_V3.TEACHER"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSSP_V3"
        mc:Ignorable="d"
        Background="LightGreen"
        WindowStartupLocation="CenterScreen"
        Title="TeacherMenu" Height="545.422" Width="451.928">
    <Grid>
        <Grid.RowDefinitions>

            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>

        <Button x:Name="aaa" Content="Добавить день" Grid.Row="0" FontSize="30" Click="Ozenki" IsDefault="True" Margin="83,109.2,93,79.6" 
                Background="SkyBlue"/>

        <Button x:Name="bbb" Content="Написать замечание" Grid.Row="1" FontSize="30" Click="GoNote" IsDefault="True" Margin="53,108.8,66.6,79.6" 
                Background="SkyBlue"/>

    </Grid>
</Window>
